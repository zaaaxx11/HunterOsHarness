.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.hunter/
.hunter-demo/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
*.db
*.sqlite
nul
.zcode/
.kilo/

# release posture: roadmap, plans stay untracked; tests/ IS tracked (CI/transparency)
# package artifact still excludes tests via MANIFEST.in prune; state/logs never ship.
docs/ROADMAP*.md
docs/plans/
.hunter*/
.hunter-cli-smoke/
*.log
artifacts/
reports/

# Local working rules for the operator (describes the push/commit hygiene
# above); stays on this machine and never reaches the public repo.
rules.md
backup/
